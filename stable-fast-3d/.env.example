# .env 파일로 복사하여 사용하세요.
# 이 파일은 `api_server.py`가 시작될 때 자동으로 로드됩니다.

# (선택 사항) 텍스처 베이킹 해상도. VRAM이 낮은 GPU(예: 4GB)에서는 512로 설정하는 것을 권장합니다.
# 고사양 GPU에서는 1024 또는 그 이상을 사용할 수 있습니다.
BAKE_RESOLUTION=1024

# (선택 사항) PyTorch CUDA 메모리 할당자 설정. 'CUDA out of memory' 오류가 발생할 경우 시도해볼 수 있습니다.
# 가능한 값: "expandable_segments:True"
# PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

# (예시) 향후 API 키 인증을 추가할 경우를 위한 변수
# API_KEY="your_secret_api_key_here"
